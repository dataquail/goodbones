{
  "name": "@goodbones/campaigns",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/campaigns/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["^build"],
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/build", "{projectRoot}/.tsbuildinfo"],
      "options": {
        "command": "pnpm run build",
        "cwd": "packages/campaigns"
      }
    },
    "check": {
      "executor": "nx:run-commands",
      "cache": true,
      "outputs": ["{projectRoot}/.tsbuildinfo", "{projectRoot}/build/src"],
      "options": {
        "command": "pnpm run check",
        "cwd": "packages/campaigns"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "command": "pnpm run test",
        "cwd": "packages/campaigns"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "command": "pnpm exec oxlint --type-aware --quiet packages/campaigns",
        "cwd": "{workspaceRoot}"
      }
    }
  },
  "tags": []
}
